<?xml version="1.0"?>
<clickhouse>
    <logger>
        <level>information</level>
        <console>1</console>
    </logger>

    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>

    <listen_host>::</listen_host>

    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>

    <users_config>users.xml</users_config>

    <default_database>mapexos</default_database>

    <timezone>UTC</timezone>

    <!-- Memory sizing for the small/eval profile (container limit in
         docker-compose.yml). The cap is kept below the container limit because
         in a cgroup the server's memory tracker also counts reclaimable page
         cache and jemalloc-retained pages; MALLOC_CONF (in compose) returns
         freed pages to the OS promptly. Keep the mark cache small and the
         secondary caches off to keep the idle footprint low. -->
    <max_server_memory_usage>943718400</max_server_memory_usage>
    <mark_cache_size>67108864</mark_cache_size>
    <uncompressed_cache_size>0</uncompressed_cache_size>
    <mmap_cache_size>0</mmap_cache_size>
    <compiled_expression_cache_size_bytes>0</compiled_expression_cache_size_bytes>

    <!-- System-log retention for the small/eval profile. trace_log (query
         profiler sampling) is disabled — metrics are already exposed via the
         Prometheus endpoint — while the remaining logs collect less often and
         carry a short TTL so they stay small and bounded on a long-lived
         container. -->
    <trace_log remove="1"/>
    <metric_log>
        <collect_interval_milliseconds>30000</collect_interval_milliseconds>
        <ttl>event_date + INTERVAL 2 DAY DELETE</ttl>
    </metric_log>
    <asynchronous_metric_log>
        <ttl>event_date + INTERVAL 2 DAY DELETE</ttl>
    </asynchronous_metric_log>
    <part_log>
        <ttl>event_date + INTERVAL 2 DAY DELETE</ttl>
    </part_log>
    <query_log>
        <ttl>event_date + INTERVAL 3 DAY DELETE</ttl>
    </query_log>
    <text_log>
        <ttl>event_date + INTERVAL 2 DAY DELETE</ttl>
    </text_log>
</clickhouse>
